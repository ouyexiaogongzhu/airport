{
  "name": "rfplay-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:schema": "wrangler d1 execute rfplay --local --file=migrations/0001_schema.sql",
    "db:schema:remote": "wrangler d1 execute rfplay --remote --file=migrations/0001_schema.sql"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "@types/bcryptjs": "^2.4.6",
    "typescript": "^5.6.0",
    "vitest": "^5.0.0",
    "wrangler": "^4.0.0"
  }
}
